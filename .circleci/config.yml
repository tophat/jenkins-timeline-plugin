version: 2
jobs:
  test:
    docker:
      - image: circleci/openjdk:8-jdk-node-browsers-legacy
    steps:
      - checkout
      - run: make clean_install
      - run: make test
  build:
    docker:
      - image: circleci/openjdk:8-jdk-node-browsers-legacy
    steps:
      - checkout
      - run: make build
          
workflows:
  version: 2
  build_and_test:
      jobs:
          - build
          - test
